[ sh snippet ]
	#! if elif for wh until case

[ c snippet ]
    main inc  Inc Def def ifdef #if once if  el   t do wh for forr
    fun  fund td  st  tds tde   pr  fpr  prd fprd . un

[ jQuery snippet ]
    addClass    append   dblclick focus        load      ready       size
    add         appendTo each     getjson      mdown     removeattr  stoggle
    after       attrm    el       getscript    mmove     removeclass submit
    ajaxerror   attr     eltrim   get          mout      remove      sup
    ajaxgetif   before   end      height       mover     reset       text
    ajaxget     bind     error    hidec        mup       resize      this
    ajaxpost    blur     fadeinc  hide         next      scroll      togclass
    ajaxsend    change   fadein   hover        not       sdownc      togsh
    ajaxsetup   children fadeoutc html         one       sdown       tog
    ajax        click    fadeout  insertAfter  parent    sdupc       trig
    ajaxstart   clone    fadetoc  insertBefore parents   select      unbind
    ajaxstop    contains fadeto   is           prepend   showc       val
    ajaxsuccess cssm     filter   loadf        prependto show        width
    animate     css      find     loadif       prev      sib         wrap

[ javascript snippet ]
    proto fun  f  if ife switch  case t
    for   forr wh do :f  timeout gett

[ vim snippet ]
	header guard f for wh if ife

